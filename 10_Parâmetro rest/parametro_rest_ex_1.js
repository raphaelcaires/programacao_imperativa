//parâmetro rest
function numeros(...arg)

    {
       // console.log(arg.length);
       let valor=arg;
       //console.log(arg)
       console.log(valor);
    }

numeros();
numeros(5);
numeros(1,2,3,4);
